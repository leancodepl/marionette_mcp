name: marionette_cli
description: CLI for multi-instance Flutter app interaction via Marionette.
version: 0.1.0
resolution: workspace

executables:
  marionette: marionette

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  logging: ^1.3.0
  marionette_mcp:
  path: ^1.9.0
  vm_service: ^15.0.0
