name: marionette_logger
description: Log collector for Marionette that integrates with the logger package.
version: 0.4.0
repository: https://github.com/leancodepl/marionette_mcp
issue_tracker: https://github.com/leancodepl/marionette_mcp/issues
resolution: workspace

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ^2.0.0
  marionette_flutter: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
