name: marionette_logger
description: Flutter extensions for AI agent interaction via MCP — lets Claude, Copilot, and Cursor tap, scroll, type, and screenshot your app.
version: 0.3.0
repository: https://github.com/leancodepl/marionette-mcp
issue_tracker: https://github.com/leancodepl/marionette-mcp/issues
topics:
  - mcp
  - ai
  - claude
  - agent
  - copilot
resolution: workspace

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ^2.0.0
  marionette_flutter: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
