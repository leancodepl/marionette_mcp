name: marionette_logging
description: Log collector for Marionette that integrates with the logging package.
version: 0.3.0
repository: https://github.com/leancodepl/marionette-mcp
issue_tracker: https://github.com/leancodepl/marionette-mcp/issues
resolution: workspace

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  marionette_flutter: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
