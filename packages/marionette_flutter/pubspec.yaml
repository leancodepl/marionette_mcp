name: marionette_flutter
description: Flutter extensions enabling AI agents to inspect and interact with running Flutter apps via Marionette MCP.
version: 0.4.0
repository: https://github.com/leancodepl/marionette_mcp
issue_tracker: https://github.com/leancodepl/marionette_mcp/issues
resolution: workspace

environment:
  sdk: "^3.6.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
